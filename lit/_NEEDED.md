# _NEEDED — papers the murderboard could not download

Auto-appended by `fetch_paper.py` when a fetch fails or hits a paywalled host, or by `--need` for a citation with no reachable URL. A human can get any of these — grab the PDF, drop it in the library, and delete the line.

